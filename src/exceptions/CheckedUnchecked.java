package exceptions;

public class CheckedUnchecked {

    // unchecked exceptions are not verified by the compiler.
    // RuntimeException and all of its subclasses are unchecked exceptions.

    // Checked: When there's a possibility of recovery
    // Unchecked: When there isn't anything that can be done

}
